<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimaryDark">@color/colorPulms</item>
    </style>

    <style name="RegistTheme" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/colorDarkgray</item>
        <item name="colorControlActivated">@color/colorPulms</item>
    </style>

    <!--Dialog样式-->
    <style name="ActivityDialogStyle" >
        <!--去掉action bar和标题栏-->
        <item name="android:windowActionBar">false</item>
        <item name="android:windowNoTitle">true</item>
        <!--背景透明-->
        <item name="android:windowIsTranslucent">true</item>
        <!--dialog圆角-->
        <item name="android:windowBackground">@drawable/dialog_autologin_bg</item>

        <!--弹窗后整个屏幕的背景是否有遮障层，默认是半透明的-->
        <item name="android:backgroundDimEnabled">true</item>
    </style>

</resources>
